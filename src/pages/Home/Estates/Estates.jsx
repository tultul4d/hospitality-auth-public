

const Estates = ({cards}) => {
    const {estate_title} = cards;
    return (
        
        <div>
        <h2 className="8xl">{estate_title}</h2>
        </div>
    );
};

export default Estates;
